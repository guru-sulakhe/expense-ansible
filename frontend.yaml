- name: configure frontend service
  become: yes
  hosts: frontend
  tasks:
    - name: install nginx
      ansible.builtin.dnf:
        name: nginx
        state: latest

    - name: start and enable nginx
      ansible.builtin.service:
        name: nginx
        state: installed
        enabled: yes
      